# Gym

A local-first Expo React Native app for logging daily body weight.

## Getting Started

```sh
npm install
npm test
npm start
```

The app stores weight entries on the device with AsyncStorage. There is no login,
backend, cloud sync, or reminder system in this MVP.
